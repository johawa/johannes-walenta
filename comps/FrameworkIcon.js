import css from '../styles/frameworkIcon.css';

const FrameworkIcon = (props) => {
    return (
        <div style={{color: 'white' }} className={css.FrameworkIcon}>Framework Icon</div>
    )
}

export default FrameworkIcon;